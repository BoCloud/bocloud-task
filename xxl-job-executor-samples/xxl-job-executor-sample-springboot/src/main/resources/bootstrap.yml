spring:
  application:
      name: xxl-job-executor-demo
  cloud:
    config:
        enabled: false
    consul:
      host: 10.20.11.111
      port: 8500
      enabled: true
      discovery:
        enabled: true
        register: true
        serviceName: xxl-job-executor-demo
        healthCheckPath: /health
        healthCheckInterval: 15s
        healthCheckDeregisterCriticalServiceAfter: 30s
        prefer-ip-address: true
        tags: urlprefix-/${spring.application.name},app_test1,zh_zhangsan,apolloapp_001
        ##instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${spring.cloud.client.host}:${server.port}}}
        instanceId: ${spring.application.name}:${spring.cloud.client.hostname}:${spring.application.instance_id:${server.port}}
        acl-token: 5b37535b-216e-2e35-6116-30f65d4454e1
      config:
        enabled: true #默认是true
        format: YAML    # 表示consul上面文件的格式 有四种 YAML PROPERTIES KEY-VALUE FILES
        data-key: paasconfig #表示consul上面的KEY值(或者说文件的名字) 默认是data
        watch:
          enabled: true #实时刷新配置
        prefix: #设置配置值的基本文件夹
        default-context: #设置所有应用程序使用的文件夹名称
        profile-separator: #设置用于使用配置文件在属性源中分隔配置文件名称的分隔符的值
        acl-token: p2BE1AtpwPbrxZdC6k+eXA==